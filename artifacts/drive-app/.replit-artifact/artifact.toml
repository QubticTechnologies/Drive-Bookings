kind = "web"
previewPath = "/"
title = "DriveApp"
version = "1.0.0"
id = "artifacts/drive-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24190

[services.development]
run = "pnpm --filter @workspace/drive-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/drive-app", "run", "build" ]
publicDir = "artifacts/drive-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24190"
BASE_PATH = "/"
