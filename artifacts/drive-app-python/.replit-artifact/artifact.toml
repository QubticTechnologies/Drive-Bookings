kind = "web"
  previewPath = "/streamlit/"
  title = "DriveApp Python (Streamlit)"
  version = "1.0.0"
  id = "artifacts/drive-app-python"
  router = "path"

  [[services]]
  name = "web"
  paths = ["/streamlit"]
  localPort = 5001

  [services.development]
  run = "streamlit run app.py --server.port 5001 --server.baseUrlPath /streamlit"

  [services.env]
  PORT = "5001"
  BASE_PATH = "/streamlit/"
  